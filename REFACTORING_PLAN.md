# LaunchDock - План рефакторинга архитектуры

## 📋 Текущее состояние

### Проблемы:
1. ✅ **ContentView.swift** — 534 строки, слишком много ответственности
2. ✅ **ApplicationManager** — смешивает логику сканирования и запуска
3. ✅ **FolderManager** — смешивает бизнес-логику и хранение данных
4. ⚠️ Отсутствует чёткое разделение на слои

---

## 🎯 Предложенная архитектура

```
LaunchDock/
├── Models/                      # Модели данных
│   ├── AppInfo.swift           ✅ Уже есть
│   └── VirtualFolder.swift     ✅ Уже есть
│
├── Services/                    # Бизнес-логика (без UI)
│   ├── AppLaunchService.swift  ✅ СОЗДАН - Запуск приложений
│   ├── AppScannerService.swift ✅ СОЗДАН - Сканирование файловой системы
│   └── StorageService.swift    ✅ СОЗДАН - Сохранение/загрузка данных
│
├── ViewModels/                  # Управление состоянием (MVVM)
│   ├── ApplicationManager.swift  📝 Рефакторинг (переместить сюда)
│   ├── FolderManager.swift      📝 Рефакторинг (переместить сюда)
│   └── ContentViewModel.swift   🆕 СОЗДАТЬ - Состояние ContentView
│
├── Views/                       # UI компоненты
│   ├── ContentView.swift       📝 Упростить (удалить логику)
│   ├── HeaderView.swift        ✅ СОЗДАН
│   ├── StatisticsView.swift    ✅ СОЗДАН
│   ├── DragDropOverlay.swift   ✅ СОЗДАН
│   ├── AppIconView.swift       ✅ Уже есть
│   ├── FolderView.swift        ✅ Уже есть
│   ├── FolderAppsView.swift    ✅ Уже есть
│   ├── HiddenAppsView.swift    ✅ Уже есть
│   ├── SearchBar.swift         ✅ Уже есть
│   ├── SettingsView.swift      ✅ Уже есть
│   └── [Sheets]/               ✅ Уже есть
│
└── Utils/                       # Утилиты и расширения
    ├── Bundle+Extensions.swift  ✅ Уже есть
    ├── SettingsManager.swift   ✅ Уже есть
    └── DragDropHandler.swift   ✅ СОЗДАН
```

---

## 🔨 Пошаговый план рефакторинга

### ✅ Этап 1: Создание структуры (ЗАВЕРШЕНО)
- [x] Создать папки `Services/` и `ViewModels/`
- [x] Создать `AppLaunchService.swift`
- [x] Создать `AppScannerService.swift`
- [x] Создать `StorageService.swift`
- [x] Создать `HeaderView.swift`
- [x] Создать `StatisticsView.swift`
- [x] Создать `DragDropOverlay.swift`
- [x] Создать `DragDropHandler.swift`

### 📝 Этап 2: Рефакторинг ApplicationManager
**Цель**: Упростить и перенести в ViewModels/

```swift
// БЫЛО в Utils/ApplicationManager.swift:
- Сканирование директорий
- Создание AppInfo
- Запуск приложений
- Фильтрация
- Поиск

// ДОЛЖНО СТАТЬ в ViewModels/ApplicationManager.swift:
class ApplicationManager: ObservableObject {
    @Published var applications: [AppInfo] = []
    @Published var isLoading = true
    @Published var searchText = ""
    
    private let scanner: AppScannerService
    private let launcher: AppLaunchService
    private var folderManager: FolderManager?
    
    // Только координация и состояние
    // Вся логика делегируется сервисам
}
```

**Действия**:
1. Переместить `ApplicationManager.swift` из `Utils/` в `ViewModels/`
2. Заменить прямое сканирование на вызовы `AppScannerService`
3. Заменить прямой запуск на вызовы `AppLaunchService`
4. Удалить методы `scanDirectory()` и `createAppInfo()` - они теперь в сервисе

---

### 📝 Этап 3: Рефакторинг FolderManager
**Цель**: Разделить бизнес-логику и хранение данных

```swift
// БЫЛО в Utils/FolderManager.swift:
- Управление папками
- Управление скрытыми приложениями
- Сохранение в JSON
- Загрузка из JSON
- Экспорт/импорт

// ДОЛЖНО СТАТЬ в ViewModels/FolderManager.swift:
class FolderManager: ObservableObject {
    @Published var folders: [VirtualFolder] = []
    @Published var hiddenAppPaths: Set<String> = []
    
    private let storage: StorageService
    
    // Только бизнес-логика и координация
    // Хранение делегируется StorageService
}
```

**Действия**:
1. Переместить `FolderManager.swift` из `Utils/` в `ViewModels/`
2. Заменить прямую работу с JSON на вызовы `StorageService`
3. Удалить методы `saveFolders()`, `loadFolders()`, `saveHiddenApps()`, `loadHiddenApps()`
4. Использовать `storage.saveFolders()` и `storage.loadFolders()` вместо этого

---

### 📝 Этап 4: Упрощение ContentView
**Цель**: Уменьшить с 534 строк до ~150-200

**ContentView ДОЛЖЕН содержать только**:
- Компоновку UI
- Навигацию между экранами
- Передачу данных в компоненты

**ContentView НЕ ДОЛЖЕН содержать**:
- Логику drag & drop → DragDropHandler
- Построение меню → HeaderView
- Статистику → StatisticsView
- Обработку событий запуска → AppLaunchService

```swift
struct ContentView: View {
    @StateObject private var viewModel = ContentViewModel()
    
    var body: some View {
        VStack(spacing: 20) {
            HeaderView(...)
            
            if viewModel.isLoading {
                ProgressView()
            } else {
                AppsGridView(...)
            }
            
            StatisticsView(...)
        }
        .overlay(DragDropOverlay(...))
        .onDrop { viewModel.handleDrop($0) }
        .sheet(...)  // Все модальные окна
    }
}
```

**Действия**:
1. Создать `ContentViewModel` с состоянием
2. Переместить все `@State` переменные в ViewModel
3. Заменить встроенные view на отдельные компоненты
4. Делегировать drag & drop в `DragDropHandler`

---

## 🎨 Преимущества новой архитектуры

### 1. **Разделение ответственности (Single Responsibility)**
- Services — чистая логика без UI
- ViewModels — управление состоянием
- Views — только отображение

### 2. **Тестируемость**
```swift
// Теперь можно тестировать без UI:
let scanner = AppScannerService()
let apps = scanner.scanAllApplications()
XCTAssertGreaterThan(apps.count, 0)
```

### 3. **Переиспользование**
```swift
// Один сервис можно использовать везде:
let launcher = AppLaunchService()
launcher.launchApplication(app)  // из любого места
```

### 4. **Легче найти ошибки**
- Ошибка сканирования? → `AppScannerService.swift`
- Ошибка запуска? → `AppLaunchService.swift`
- Ошибка сохранения? → `StorageService.swift`
- UI проблема? → Смотрим только Views

### 5. **Упрощение рефакторинга**
- Каждый файл < 200 строк
- Легко понять назначение файла
- Изменения в одном месте не ломают другое

---

## ⚠️ Важные замечания

### 1. Добавление файлов в Xcode проект
После создания новых файлов через VS Code, нужно:
1. Открыть `LaunchDock.xcodeproj` в Xcode
2. Правой кнопкой на группу → "Add Files to LaunchDock..."
3. Выбрать новые файлы
4. ✅ Убедиться, что галочка "Target: LaunchDock" включена

### 2. Ошибки компиляции
Текущие ошибки типа "Cannot find 'AppInfo' in scope" решаются:
- Добавлением файлов в Xcode Target
- Пересборкой проекта (Cmd + B)

### 3. Порядок интеграции
**НЕ МЕНЯТЬ** всё сразу! Интегрировать постепенно:
1. Сначала один сервис
2. Проверить, что компилируется
3. Протестировать
4. Затем следующий

---

## 📊 Метрики успеха

### До рефакторинга:
- ContentView: **534 строки** 😱
- ApplicationManager: **~140 строк**
- FolderManager: **~240 строк**

### После рефакторинга (цель):
- ContentView: **~150 строк** ✅
- ApplicationManager: **~80 строк** ✅
- FolderManager: **~100 строк** ✅
- AppScannerService: **~90 строк** ✅
- AppLaunchService: **~45 строк** ✅
- StorageService: **~170 строк** ✅
- HeaderView: **~95 строк** ✅
- StatisticsView: **~25 строк** ✅
- DragDropOverlay: **~40 строк** ✅
- DragDropHandler: **~130 строк** ✅

**Итого**: Больше файлов, но каждый проще и понятнее!

---

## 🚀 Следующие шаги

1. **Открыть Xcode** и добавить все новые файлы в проект
2. **Рефакторинг ApplicationManager** — заменить внутренности на сервисы
3. **Рефакторинг FolderManager** — делегировать хранение в StorageService
4. **Создать ContentViewModel** — перенести состояние из ContentView
5. **Упростить ContentView** — использовать новые компоненты

---

## 💡 Дополнительные улучшения (опционально)

### 1. Создать Extensions/
```
Extensions/
├── NSImage+Extensions.swift   # Работа с иконками
├── URL+Extensions.swift       # Утилиты для путей
└── View+Extensions.swift      # SwiftUI модификаторы
```

### 2. Создать Protocols/
```swift
protocol AppLaunching {
    func launchApplication(_ app: AppInfo) -> Bool
}

protocol AppScanning {
    func scanAllApplications() -> [AppInfo]
}

protocol DataStorage {
    func save<T: Encodable>(_ data: T, to filename: String) throws
    func load<T: Decodable>(_ type: T.Type, from filename: String) throws -> T?
}
```

### 3. Error Handling
```swift
enum LaunchDockError: Error {
    case applicationNotFound
    case scanningFailed
    case storageFailed(Error)
    case launchFailed(Error)
}
```

---

**Создано**: 30 октября 2025  
**Автор**: AI Assistant (GitHub Copilot)  
**Проект**: LaunchDock - macOS Launchpad Replacement
